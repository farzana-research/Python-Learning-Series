# Python Fundamentals for Research (PFR)

A clean, tested collection of Python fundamentals demonstrating:
- algorithms (recursion, sorting, searching)
- basic data structures
- matrix operations
- file handling
- OOP
- exception handling
- decorators
- simple CLI app

## Setup
Requires Python 3.10+.

```bash
pip install -e .
python -m unittest -v
